<h1>Why not all these other wonderful things??</h1>
<p>So this part would show what the user chose and show them the WHOLE NEW WORLD they're missing out on (sorry not sorry :P)</p>
<%= Form.find(11).types %>
<%= Form.find(11).targets %>
<%= Form.find(11).msgs %>
<br>
<p>Three other random things you could have chosen:</p>
<% trip = make_triplet(@audience_arr, @identity_arr, @message_arr) %>
<%= trip.element1 %>
<%= trip.element2 %>
<%= trip.element3 %>
<p>Contact info section</p>
<%= form_for(Contact.new) do |f| %>
  <div>
  <%= f.email_field :email %>
  <%= f.label("Email") %>
  </div>

  <div>
  <%= f.url_field :website %>
  <%= f.label("Website") %>
  </div>

  <br>
  <%= f.submit "Create", class: "btn btn-primary" %>
<% end %>
